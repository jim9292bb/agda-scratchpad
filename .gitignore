node_modules

# build output
/build
/.svelte-kit

# env
.env
.env.*
!.env.sample

# Vite
vite.config.js.timestamp-*

# ALS WASM binaries, library archives, and pre-built .agdai cache zips
# (downloaded via `npm run setup`, or placed by hand — see file-server/README.md).
# Staged in file-server/{library,als}/, synced into static/{library,als}/
# for serving — neither is committed.
/file-server/library/
/file-server/als/
/static/library/
/static/als/

/experiments/build-library/results/*.zip

# Extracted .agdai files for on-demand serving (generated by scripts/extract-agdai-for-serving.mjs during CI)
/static/agdai/
