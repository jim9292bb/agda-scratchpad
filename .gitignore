node_modules

# build output
/build
/.svelte-kit

# env
.env
.env.*
!.env.sample

# Vite
vite.config.js.timestamp-*

# ALS WASM binaries (downloaded via `npm run setup`)
/static/als-*.wasm

# Pre-built .agdai cache zips (generated by experiments/build-library, too large for git)
/static/stdlib-agdai.zip
/static/cubical-agdai.zip
/experiments/build-library/results/*.zip
